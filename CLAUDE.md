# Claude instructions

Before declaring any implementation task finished, run `./scripts/check.sh` from the repository root. The task is not complete until every check passes. Fix any failures caused by your changes and rerun the script until it exits successfully.
